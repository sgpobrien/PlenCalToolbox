function corners_pixels = find_checker_corners(current_image)
    current_gray = rgb2gray(current_image);
    res = size(current_gray);
    for m = 1:res(1)
        for n = 1:res(2)
        end
    end
end